{{!< default}}
<div class="container">
  <div class="row">
    <h1 class="display-4">Products</h1>
  </div>
  
  <div class="row">
      {{#each products.results}}
        {{> product-card}}
      {{/each}}
  </div>
</div>
